apiVersion: v1
kind: Service
metadata:
  name: librefractals-backend
spec:
  type: ClusterIP
  selector:
    app: librefractals-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
